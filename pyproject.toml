[project]
name = "syslang"
version = "0.1.0"
description = "Human → systems-spec translator for coding models"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "SYS starter" }]

[project.scripts]
syslang = "syslang.cli:main"

[tool.setuptools.packages.find]
include = ["syslang*"]
